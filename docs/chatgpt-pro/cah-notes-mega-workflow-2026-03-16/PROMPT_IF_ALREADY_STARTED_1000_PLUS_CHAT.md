You have already been asked to create a **1000+ question** CAH question bank from the uploaded notes.

Do **not** start generating questions yet.

Reset the workflow so quality stays high and context limits do not degrade the output.

Use only the uploaded files in this chat. Do not browse. Do not use external sources.

Follow `SOURCE_PRIORITY.md` strictly:

- primary factual source: `Louisa Leone MD3 2025 - Child and Adolescent Health - Study Notes.pdf`
- secondary factual source: `CAH Bible (shared 2025).pdf`
- tertiary concept-hint source only: `cah-chatgpt-pro-source-files-2026-03-16.zip`

New task:

- treat the previous `1000+ questions` request as a **program of work**, not a single response
- first create a **master batch plan only**
- do **not** generate any SBA items yet
- break the work into many small batches so coverage is broad and quality stays high
- preferentially focus on the Louisa Leone notes whenever the two note sets overlap

Planning constraints:

- total planned questions must be **>= 1000**
- prefer **10 to 14 questions per batch**
- cover **every major subtopic** from the notes
- broad sections must be split across multiple batches
- use a mix of reasoning tasks, not only diagnosis questions
- avoid overlap-prone past-paper patterns from `REJECTED_PATTERNS_SUMMARY.md`
- do not use the question zip as a stem template

Output requirements:

Return **markdown only** with:

1. a refined topic inventory grouped by major section
2. estimated question counts per section
3. a staged batch schedule that reaches **>= 1000** questions
4. for each batch:
   - batch id
   - curriculum area
   - topic cluster
   - exact subtopics
   - target question count
   - preferred question style mix
   - source priority notes
   - overlap-risk warning if relevant
5. the total planned question count
6. the single best batch to generate first

Do not generate any questions in this step. Return the plan only.
