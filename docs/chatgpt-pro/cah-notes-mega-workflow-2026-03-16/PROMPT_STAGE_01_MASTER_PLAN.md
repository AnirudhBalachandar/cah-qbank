You are building a **1000+ question** Child and Adolescent Health SBA workflow for Sydney Medical School.

Use only the uploaded files in this chat. Do not browse. Do not use external sources.

Follow `SOURCE_PRIORITY.md` strictly:

- primary factual source: `Louisa Leone MD3 2025 - Child and Adolescent Health - Study Notes.pdf`
- secondary factual source: `CAH Bible (shared 2025).pdf`
- tertiary concept-hint source only: `cah-chatgpt-pro-source-files-2026-03-16.zip`

Task:

- create a **master plan only**
- do **not** generate questions yet
- use `LOUISA_TOPIC_MAP.md` as a starting scaffold, but refine it from the PDFs where helpful
- plan a workflow that will produce **at least 1000 full questions total**
- break the work into **many small batches** so quality remains high

Planning constraints:

- prefer **10 to 14 questions per batch**
- broad topics must be split over multiple batches
- use a mix of question tasks, not only diagnosis questions
- prioritise note-derived reasoning tasks over question-bank style paraphrases
- avoid the overlap-prone patterns in `REJECTED_PATTERNS_SUMMARY.md`
- do not use the question zip as a writing template

Output requirements:

Return a **master batch plan in markdown only** with:

1. a refined topic inventory grouped by major note section
2. suggested target question counts per major section
3. a batch schedule with enough batches to reach **>= 1000** total questions
4. for each batch:
   - batch id
   - curriculum area
   - topic cluster
   - exact subtopics
   - target question count
   - preferred question style mix
   - source priority notes
   - overlap-risk warning if relevant
5. a final total planned question count

Do not generate any SBA items yet. Return the plan only.
